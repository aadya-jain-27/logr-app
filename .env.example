# Google Gemini API key. Get a free one at https://aistudio.google.com/apikey
# Read only on the server (API routes / dev middleware). Never exposed to the browser.
GEMINI_API_KEY=your_free_gemini_key_here
