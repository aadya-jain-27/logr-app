# Copy this file to ".env" and paste your free Google Gemini key.
# Get one in ~2 minutes (no card): https://aistudio.google.com  ->  "Get API key"
# The .env file is gitignored, so your key never gets committed or shipped to the browser.

GEMINI_API_KEY=your_free_gemini_key_here
